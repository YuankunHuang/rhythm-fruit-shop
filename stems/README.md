# Stems

这里存放 Demucs v4 的分轨输出缓存。

推荐用途：

- `drums.wav`：检查鼓点、重拍、节奏密度。
- `vocals.wav`：检查人声旋律和情绪段落。
- `other.wav`：检查主旋律、合成器、铺底动机。
- `bass.wav`：检查低频推进和段落能量。

`stems/` 不参与 HTML demo 运行时加载。它只服务于制谱、诊断和人工润色。
